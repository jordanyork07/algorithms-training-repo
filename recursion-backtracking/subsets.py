"""
Given a list of distinct integers nums, return all possible subsets of the array.

The order of the subsets does not matter, but each subset should be distinct and
contain unique elements from the original input.

Constraints:
- 0 <= len(nums) <= 10

Example:
nums = [1, 2, 3]
return [[], [1], [2], [3], [1, 2], [1, 3], [2, 3], [1, 2, 3]]
"""


def subsets(nums):
    raise NotImplementedError("Implement this function.")
