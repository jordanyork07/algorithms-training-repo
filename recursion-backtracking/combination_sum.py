"""
Given an array of distinct integers candidates and a target integer target, return
all unique combinations of candidates where the chosen numbers sum to target.

Each number in candidates may be used repeatedly in a single combination.

Constraints:
- 1 <= len(candidates) <= 30
- 1 <= target <= 500
- 1 <= candidates[i] <= 200

Example:
candidates = [2, 3, 6, 7]
target = 7
return [[2, 2, 3], [7]]
"""


def combination_sum(candidates, target):
    raise NotImplementedError("Implement this function.")
